APP_NAME="HerzTrace"
APP_ENV=local
APP_KEY=base64:X+j3eAhHUDSVn2RUaYbuXaYhBS5mf4kjTaCz3mV+Ucg=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=es
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=es_EC

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DEBUGBAR_ENABLED=true

LOG_CHANNEL=stack
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=mysql # Cambiar a 127.0.0.1 si se utiliza una DB en un servidor
DB_PORT=3306
DB_DATABASE=herzoft2024
DB_USERNAME=trazalem
DB_PASSWORD_FILE=/run/secrets/db_password
DB_ROOT_PASSWORD_FILE=/run/secrets/db_root_password

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_DRIVER=log
FILESYSTEM_DRIVER=local
QUEUE_CONNECTION=

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587 #465
MAIL_USERNAME=herzoftgroup@gmail.com
MAIL_PASSWORD=herzoft2024
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=herzoftgroup@gmail.com
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1

MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
MIX_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

VITE_APP_NAME="${APP_NAME}"

## Configuraciones Docker
FORWARD_DB_PORT=3306
FORWARD_REDIS_PORT=6379
FORWARD_PHPMYADMIN_PORT=8081
DATA_PATH_HOST=~/.sail/data
DOCKER_HOST_IP=172.17.0.1

# Configuraciones de Nginx
NGINX_HOST_HTTP_PORT=80
NGINX_HOST_HTTPS_PORT=443
NGINX_FPM_CONTAINER=app
NGINX_FPM_PORT=9000

# Datos del Dominio
DOMAIN=example.com # Sin el www.
EMAIL=mail@example.com

# Configuraciones de respaldo
RUTA_RESPALDO=/opt/docker/backup
CONTRASENA_RESPALDO=PonAquiUnaContrasenaSegura

# Aquí configura la versión de herzoft que quieres usar
APP_VERSION=latest 