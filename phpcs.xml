<?xml version="1.0"?>
<ruleset name="Laravel-PSR12">
    <description>PSR-12 coding standard for a Laravel project</description>

    <!-- Scan these paths -->
    <file>app</file>
    <file>config</file>
    <file>database</file>
    <file>Modules</file>
    <file>routes</file>
    <file>tests</file>

    <!-- Always skip generated / third-party code -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/storage/*</exclude-pattern>
    <exclude-pattern>*/bootstrap/cache/*</exclude-pattern>
    <exclude-pattern>*/_ide_helper*.php</exclude-pattern>
    <exclude-pattern>*/public/*</exclude-pattern>

    <!-- Base standard -->
    <rule ref="PSR12"/>

    <!-- Laravel uses <?php without a closing tag — this is valid in PSR-12,
         but the closing tag sniff fires on some PHPCS builds. Suppress it. -->
    <rule ref="PSR2.Files.ClosingTag">
        <severity>0</severity>
    </rule>

    <!-- Laravel Blade-compiled views and some helpers exceed 120 chars;
         keep the line-length check but raise the limit slightly. -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="0"/>
        </properties>
    </rule>
</ruleset>
